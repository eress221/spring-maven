<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--suppress ALL -->
<mapper namespace="com.eress.springmaven.mapper.LoginMapper">

    <select id="getUserInfo" parameterType="com.eress.springmaven.model.UserDTO" resultType="com.eress.springmaven.model.UserDTO">
--         SELECT *
--         FROM DB;
    </select>

</mapper>